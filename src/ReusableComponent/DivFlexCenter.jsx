import React from "react";

import { Box } from "@mui/material";

import styled from "@emotion/styled";

const DivFlexCenter = styled(Box)(({
    display:'flex',
    justifyContent:'center',
    alignItems:'center'
}))

export default DivFlexCenter