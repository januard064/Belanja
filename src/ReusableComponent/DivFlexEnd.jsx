import React from "react";

import { Box } from "@mui/material";

import styled from "@emotion/styled";

const DivFlexEnd = styled(Box)(({
    display:'flex',
    justifyContent:'flex-end',
    alignItems:'center'
}))


export default DivFlexEnd