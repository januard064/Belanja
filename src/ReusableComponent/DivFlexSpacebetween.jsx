import React from "react";

import { Box } from "@mui/material";

import styled from "@emotion/styled";

const DivFlexSpacebetween = styled(Box)(({
    display:'flex',
    justifyContent:'space-between',
    alignItems:'center'
}))

export default DivFlexSpacebetween