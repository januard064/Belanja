import React from "react";

import { Box } from "@mui/material";

import styled from "@emotion/styled";

const DivFlexStart = styled(Box)(({
    display:'flex',
    justifyContent:'flex-start',
    alignItems:'center'
}))

export default DivFlexStart